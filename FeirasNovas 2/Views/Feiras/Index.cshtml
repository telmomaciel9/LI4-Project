@model IEnumerable<FeirasNovas.Models.Feiras>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Categoria)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserNames)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Categoria)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserNames)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idFeira">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idFeira">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idFeira">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
